<Page x:Class="figma_design_system_uno.Presentation.HeroBasicSectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:figma_design_system_uno.Presentation"
      xmlns:controls="using:figma_design_system_uno.Controls"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{StaticResource DefaultBackgroundBrush}">

  <Page.Resources>
    <local:HeroBasicSectionModel x:Key="ViewModel" />
  </Page.Resources>

  <ScrollViewer>
    <utu:AutoLayout Orientation="Vertical"
                    Padding="32"
                    Spacing="24"
                    DataContext="{StaticResource ViewModel}">

      <!-- Controls -->
      <utu:AutoLayout Orientation="Vertical" Spacing="16">
        <TextBlock Text="Hero Basic Section"
                   FontSize="32"
                   FontWeight="Bold" />
        
        <TextBlock Text="Toggle between desktop and mobile views to see responsive hero behavior"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource SecondaryTextBrush}" />

        <!-- Device Mode Toggle -->
        <utu:AutoLayout Orientation="Horizontal" Spacing="16" PrimaryAxisAlignment="Start">
          <RadioButton Content="Desktop (1200px)"
                       IsChecked="{Binding DeviceMode, Mode=TwoWay, Converter={StaticResource EqualsConverter}, ConverterParameter=Desktop}"
                       GroupName="DeviceMode" />
          <RadioButton Content="Mobile (375px)"
                       IsChecked="{Binding DeviceMode, Mode=TwoWay, Converter={StaticResource EqualsConverter}, ConverterParameter=Mobile}"
                       GroupName="DeviceMode" />
        </utu:AutoLayout>
      </utu:AutoLayout>

      <!-- Device Frame -->
      <Border BorderBrush="{StaticResource NeutralSecondaryBrush}"
              BorderThickness="2"
              CornerRadius="4"
              Width="{Binding DeviceWidth}"
              HorizontalAlignment="Center"
              Background="White">
        
        <!-- Hero Component -->
        <controls:HeroBasicControl IsMobile="{Binding DeviceMode, Converter={StaticResource EqualsConverter}, ConverterParameter=Mobile}" />
      </Border>
    </utu:AutoLayout>
  </ScrollViewer>
</Page>
