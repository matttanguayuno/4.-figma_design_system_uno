<Page x:Class="figma_design_system_uno.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:figma_design_system_uno.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <ScrollViewer IsTabStop="True">
    <Grid utu:SafeArea.Insets="VisibleBounds">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <utu:NavigationBar Content="{Binding Title}" />

        <StackPanel Grid.Row="1"
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Spacing="16">
        <TextBox Text="{Binding Name, Mode=TwoWay}"
          PlaceholderText="Enter your name:" />
        <Button Content="Go to Second Page"
          AutomationProperties.AutomationId="SecondPageButton"
          Command="{Binding GoToSecond}" />
        <Button Content="Figma Component List"
          Command="{Binding GoToFigmaComponentList}" />

        <!-- Components Section -->
        <TextBlock Text="Components" Style="{StaticResource HeadingTextBlockStyle}" Margin="0,24,0,8" />
        <StackPanel Spacing="8">
          <Button Content="Tag"
              Command="{Binding GoToTag}" />
          <Button Content="Tag Toggle"
              Command="{Binding GoToTagToggle}" />
        </StackPanel>
        </StackPanel>
    </Grid>
  </ScrollViewer>
</Page>

