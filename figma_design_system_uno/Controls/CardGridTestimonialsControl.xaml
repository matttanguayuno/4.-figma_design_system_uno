<UserControl x:Class="figma_design_system_uno.Controls.CardGridTestimonialsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:utu="using:Uno.Toolkit.UI">

    <Grid>
        <!-- Desktop View -->
        <utu:AutoLayout Orientation="Vertical"
                        Spacing="48"
                        Padding="64"
                        HorizontalAlignment="Center"
                        Visibility="{Binding IsMobile, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            
            <!-- Heading Section -->
            <utu:AutoLayout Orientation="Vertical" Spacing="8">
                <TextBlock Text="Heading"
                           Style="{StaticResource HeadingTextBlockStyle}" />
                <TextBlock Text="Subheading"
                           Style="{StaticResource SubheadingTextBlockStyle}"
                           Foreground="{ThemeResource TextSecondaryBrush}" />
            </utu:AutoLayout>

            <!-- Cards Grid - wrapping grid -->
            <VariableSizedWrapGrid MaximumRowsOrColumns="3"
                                  Orientation="Horizontal"
                                  ItemWidth="357"
                                  ItemHeight="NaN">
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        Margin="0,0,48,48">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/64/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
                    
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource OutlineBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius200}"
                            Padding="24"
                            Margin="0,0,48,48">
                        <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/65/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
                    
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource OutlineBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius200}"
                            Padding="24"
                            Margin="0,0,48,48">
                        <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/91/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
                    
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource OutlineBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius200}"
                            Padding="24"
                            Margin="0,0,48,48">
                        <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/177/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
                    
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource OutlineBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius200}"
                            Padding="24"
                            Margin="0,0,48,48">
                        <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/247/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
                    
                    <Border Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource OutlineBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource Radius200}"
                            Padding="24"
                            Margin="0,0,48,48">
                        <utu:AutoLayout Orientation="Vertical" Spacing="24">
                            <!-- Quote -->
                            <TextBlock Text="&quot;Quote&quot;"
                                       Style="{StaticResource CardHeadingTextBlockStyle}"
                                       TextWrapping="Wrap" />
                            
                            <!-- Avatar Block -->
                            <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                                <!-- Avatar -->
                                <Ellipse Width="40" Height="40">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="https://picsum.photos/id/433/200/200" Stretch="UniformToFill" />
                                    </Ellipse.Fill>
                                </Ellipse>
                                
                                <!-- Info -->
                                <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                    <TextBlock Text="Title"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#757575" />
                                    <TextBlock Text="Description"
                                               FontFamily="{StaticResource InterFontFamily}"
                                               FontSize="16"
                                               FontWeight="Normal"
                                               Foreground="#B3B3B3" />
                                </utu:AutoLayout>
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </Border>
            </VariableSizedWrapGrid>
        </utu:AutoLayout>

        <!-- Mobile View -->
        <utu:AutoLayout Orientation="Vertical"
                        Spacing="24"
                        Padding="24"
                        HorizontalAlignment="Stretch"
                        Visibility="{Binding IsMobile, Converter={StaticResource BoolToVisibilityConverter}}">
            
            <!-- Heading Section -->
            <utu:AutoLayout Orientation="Vertical" Spacing="8">
                <TextBlock Text="Heading"
                           Style="{StaticResource HeadingTextBlockStyle}" />
                <TextBlock Text="Subheading"
                           Style="{StaticResource SubheadingTextBlockStyle}"
                           Foreground="{ThemeResource TextSecondaryBrush}" />
            </utu:AutoLayout>

            <!-- Cards List - vertical stacked -->
            <utu:AutoLayout Orientation="Vertical" Spacing="24">
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/64/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/65/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/91/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/177/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/247/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/433/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
                
                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource OutlineBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource Radius200}"
                        Padding="24"
                        HorizontalAlignment="Stretch">
                    <utu:AutoLayout Orientation="Vertical" Spacing="24">
                        <!-- Quote -->
                        <TextBlock Text="&quot;Quote&quot;"
                                   Style="{StaticResource CardHeadingTextBlockStyle}"
                                   TextWrapping="Wrap" />
                        
                        <!-- Avatar Block -->
                        <utu:AutoLayout Orientation="Horizontal" Spacing="12">
                            <!-- Avatar -->
                            <Ellipse Width="40" Height="40">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="https://picsum.photos/id/433/200/200" Stretch="UniformToFill" />
                                </Ellipse.Fill>
                            </Ellipse>
                            
                            <!-- Info -->
                            <utu:AutoLayout Orientation="Vertical" Spacing="2">
                                <TextBlock Text="Title"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#757575" />
                                <TextBlock Text="Description"
                                           FontFamily="{StaticResource InterFontFamily}"
                                           FontSize="16"
                                           FontWeight="Normal"
                                           Foreground="#B3B3B3" />
                            </utu:AutoLayout>
                        </utu:AutoLayout>
                    </utu:AutoLayout>
                </Border>
            </utu:AutoLayout>
        </utu:AutoLayout>
    </Grid>
</UserControl>
